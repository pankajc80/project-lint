---
- name: For testing purpose
  hosts: localhost
  gather_facts: false
  tasks:
    # - name: Include tasks
    #   ansible.builtin.include_tasks:
    #     file: tasks/lint_files.yml

    - name: Extract username and email from Controller
      ansible.builtin.debug:
        msg: "The user is {{ awx_user_name }} with email {{ awx_user_email }}"
